import type {Dictionary} from './type';

const en: Dictionary = {
  searchUser: {
    searchPlaceHolder: 'Search user',
  },
  profile: {
    title: 'Profile',
    noDescription: 'no description',
  },
};

export default en;
